# Supervised Learning

This directory contains implementations of various supervised learning algorithms using popular machine learning libraries such as Scikit-learn and XGBoost. The notebooks cover a range of algorithms including decision trees, random forests, gradient boosting, and XGBoost, applied to different datasets for classification and regression tasks.